package multipleclassinterface;

public interface CarCompany{
	//this is an abstract method in interface we can't use abstract keyword
	public String getName();
///in interface this needs 100% abstraction we can't use non abstact methods
	//refer interface pkg
}
