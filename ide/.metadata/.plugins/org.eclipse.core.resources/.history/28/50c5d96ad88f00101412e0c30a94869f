package function;

public class Staticdemo {
	public static void main(String[] args) {
//		staticblock block=new staticblock();
		Staticblock block=new Staticblock();
		System.out.println("inside main");
		
		
		//in another class if create a main function and called a static funtion which in another class then , when object is created this time static block is created
		
		//if we are having multiple static function they are sequetially printed when object is created 
	}
}
